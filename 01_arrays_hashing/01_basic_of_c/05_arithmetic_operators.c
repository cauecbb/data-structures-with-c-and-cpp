#include <stdio.h>

int main()
{

    // arithmetic operators

    // +  addiction
    // -  subtraction
    // *  multiplication
    // /  division
    // %  modulus
    // ++ increment
    // -- decrement
    // x+=1 = (x = x + 1) augmented assignment operators (shortcut)

    int x = 5;
    int y = 2;

    float z = x / (float) y;

    printf("%.2f", z);

    return 0;
}